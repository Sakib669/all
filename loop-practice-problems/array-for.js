let counties = ['bd', 'india', 'uk', 'us', 'russia'];
let numbers = [1, 3, 2, 4, 5, 6, 7, 8, 21, 43, 74, 88];
for (let i = 0; i < counties.length; i++) {
    const element = counties[i];
    // let arr = [element];
    console.log(element);
}